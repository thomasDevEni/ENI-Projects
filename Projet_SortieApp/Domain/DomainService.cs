using Domain.Entities;

namespace SortieApp.Domain
{
    public static class DomainService
    {
        /*public static IServiceCollection ConfigureDomainService(this IServiceCollection services)
        {
            //Type et durée de vie su service (Type Singleton: unique et utilisé dans toute l'application)
            //services.AddSingleton<SortieService>();
            // Register domain services
            services.AddScoped<Sortie>(); // Assuming Sortie is a domain entity
            return services;
        }*/

    }
}
